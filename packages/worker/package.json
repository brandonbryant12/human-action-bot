{
  "name": "@human-action-bot/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787 --experimental-vectorize-bind-to-prod",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "check-types": "tsc --noEmit",
    "lint": "echo 'No linter configured'",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply human-action-db"
  },
  "dependencies": {
    "@ai-sdk/google": "^1.0.0",
    "@effect/platform": "^0.72.0",
    "@effect/schema": "^0.75.0",
    "@human-action-bot/shared": "workspace:*",
    "ai": "^4.0.0",
    "drizzle-orm": "^0.45.1",
    "effect": "^3.12.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20241218.0",
    "drizzle-kit": "^0.31.8",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.99.0"
  }
}
